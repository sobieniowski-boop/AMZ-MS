services:
  - type: web
    name: amz-msg-checker-saas
    env: python
    plan: starter
    buildCommand: pip uninstall -y psycopg2 psycopg2-binary || true && pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
